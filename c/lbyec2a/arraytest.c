#include <stdio.h>   
#include <stdlib.h>  

int main() {
  int *arr = NULL;
  int size = 0;
  int capacity = 10;

  arr = malloc(capacity * sizeof(int));

  int curNum;

  while (scanf("%d", &curNum) == 0) {
    
  }
}

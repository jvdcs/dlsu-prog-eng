#include <stdio.h>
#include <string.h>

int sumArr(const int* arr, size_t len) {
  int sum = 0;
  for (size_t i = 0; i <= len; i++) {
    sum += arr[i];
  }
  return sum;
}

int main() {
  char opt[1] = "";
  int nums[100];
  int result = 0;

  // for (size_t i = 0; i <= sizeof(); inc-expression) {
  
  // }


  printf("");
}
